{
  "name": "root",
  "private": true,
  "scripts": {
    "postinstall": "cd short-links-main && npm install",
    "dev": "cd short-links-main && npm run dev",
    "start": "cd short-links-main && npm start",
    "build": "cd short-links-main && npm run build",
    "lint": "cd short-links-main && npm run lint"
  }
}
